# Public — exposed to the browser. Anon key is fine here (RLS enforces access).
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=

# Server-only — Netlify Functions. NEVER commit, NEVER prefix with VITE_
SUPABASE_SERVICE_ROLE_KEY=
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# Public — Stripe publishable key (browser)
VITE_STRIPE_PUBLISHABLE_KEY=
